本地仓库和远程仓库关联使用 git push -u
在推送的时候会自动关联起来      将本地分支和远程分支映射  以后就可以使用git push命令了


git checkout -b 本地名字名  origin/元旦分支名  也会关联


git branch -vv  可以看到本地和远程的关联情况

git branch -u origin/本地分支名   两个名称必须一致才可以 提交    否则没用

git branch -m  旧名称   新名称


使用命令 git remote show origin，可以查看remote地址，远程分支，还有本地分支与之相对应关系等信息。 stale 过期的  

远程仓库删除了一个分支  本地fetch 无法看到，本地git branch -a 仍然可以看到被删除的分支
使用上面的语句  可以看到过期的分支  然后删除


 git push origin test:master   这个命令 如果远程没有master 就会创建，  有的话  会把内容变成本地的内容
